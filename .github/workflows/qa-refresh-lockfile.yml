name: QA Current Launch Validation

on:
  push:
    branches:
      - fix/launch-validation-lockfile

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/launch-validation-lockfile
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run test:talent
      - run: npm run test:security
      - run: npm run test:marketing
      - run: npm run test:intelligence
      - run: npm run build
